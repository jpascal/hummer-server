.content-header
  %h4 Projects
.content-border
  .content-tools
    .content-tools-right
      = link_to "New project", new_project_path, :class => "btn btn-info btn-xs" if can? "projects","create"
  .content-inside
    = render "projects", :projects => @projects, :columns => [:name, :suites, :created, :owner]
