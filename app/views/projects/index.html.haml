.content-header
  %h4 Projects
  = link_to "Add", new_project_path, :class => "btn btn-info btn-xs pull-right" if can? :create, Project
.content-border
  = render "projects", :projects => @projects, :columns => [:name, :suites, :created, :owner]
