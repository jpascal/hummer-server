=link_to "Delete", project_suite_path(suite.project_id, suite), :method => :delete, :data => {:confirm => "Are you sure?"}, :class => "btn btn-danger btn-xs" if can? :destroy, suite
=link_to "Edit", edit_project_suite_path(suite.project_id, suite), :class => "btn btn-default btn-xs" if can? :update, suite
=link_to "Reload", reload_project_suite_path(suite.project_id, suite), :class => "btn btn-default btn-xs" if can? :reload, suite
=link_to "Download", suite.tempest.url, :class => "btn btn-default btn-xs"
=link_to "Compare", project_suite_compares_path(suite.project_id, suite), :class => "btn btn-info btn-xs"
=link_to "Paste2", paste_project_suite_path(suite.project_id, suite), :class => "btn btn-default btn-xs" if can? :paste, suite
